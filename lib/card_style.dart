/// Defines the visual style for repository cards
enum CardStyle {
  /// Modern card style with detailed information
  modern,

  /// Minimal list style that focuses only on essential details
  minimal,

  /// Grid layout for displaying repositories in a grid format
  grid,
}
